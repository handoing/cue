{
  "name": "cue",
  "version": "1.0.0",
  "description": "> TODO: description",
  "author": "handoing <chenhandoing@gmail.com>",
  "homepage": "https://github.com/handoing/cue/tree/master/packages/cue#readme",
  "license": "ISC",
  "main": "dist/cue.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/handoing/cue.git"
  },
  "scripts": {
    "build": "esbuild index.js --bundle --format=cjs --minify --outfile=./dist/cue.js",
    "watch": "esbuild index.js --bundle --format=cjs --watch --outfile=./dist/cue.js",
    "test": "echo \"Error: run tests from root\" && exit 1"
  },
  "bugs": {
    "url": "https://github.com/handoing/cue/issues"
  },
  "dependencies": {
    "cue-compiler": "workspace:^1.0.0",
    "cue-runtime": "workspace:^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.8.42"
  }
}
